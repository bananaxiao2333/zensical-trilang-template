# 模版经验集

这份文件记录的是**这套模版在真实站点上跑过一遍之后，攒下来的、会咬人的那些事**。

它和另外两份文档的分工：

| 文档 | 讲什么 |
| --- | --- |
| `README.md` | 这套模版怎么用：目录分层、常用命令、怎么换内容、怎么发布 |
| `TRANSLATION-GUIDE.md` | 译文怎么写、怎么算过期、验收标准 |
| **本文** | 为什么某些地方非得写成现在这样；哪些改法会**静默**出错；判据立在哪里才拦得住 |

怎么读：**先读最后一节「如果重来一次」**，那是一份可以直接照着做的清单；
再看你正要动的那一块（加语言、加版本、改模板、改 Makefile、处理大文件……）。

三条阅读约定：

1. 每一条都写成「现象 → 根因 → 判据/对策」。**没有现象支撑的话不写在这里**——
   这是文件的价值所在，写成「要注意测试」那样的话等于没写。
2. 每条尽量给出代码位置（`tools/xxx.py` 的函数名、`overrides/partials/xxx.html`、
   `Makefile` 的目标名）。位置会随重命名漂移，**判据比位置耐久**，顺着判据查。
3. 「静默」这个词在本文件里是具体的：**构建不报错、体检不报错、页面还长得挺正常，
   只是发出去的东西是错的**。绝大部分踩过的坑都属于这一类。本文的重点就是它们。

写这份文件的实例是一个 300+ 页、三语、两个版本轴、带 318 MB 归档和一份离线包的真实站点。
里面出现的数字（页数、字节数、体积）都来自那一次实测，只当例子看。

---

## 一、分层与生成物身份

### 1.1 `content/` 是唯一手写层，`docs/` 是生成物却要入库

**现象**：改了 `content/` 下的源文件，预览和线上都没变，而且**没有任何报错**。

**根因**：构建（`zensical build`、`zensical serve`）直接读 `docs/`。`docs/` 下的 `.md`
与 `.nav.yml` 是 `tools/docsgen.py`、`tools/navgen.py` 的产物；少跑一次 `make gen`，
构建就会拿仓库里那份**旧副本**去构建。整条链路里没有一处会因此报错——因为从构建的
角度看，它读到的就是「当前内容」。

**对策**：

* `make build` 的第一步是 `gen`（见 `Makefile`），CI 也一样；
* CI 另有一道**生成后比对**：`tools/docsgen.py --check` 与 `tools/navgen.py --check`
  （见 `.github/workflows/docs.yml` 的 "Verify generated tree is up to date"）。
  它检查的是「提交进仓库的 `docs/` 与 `content/` 是否一致」，不一致就失败，
  而不是把半成品发出去；
* 所以 `docs/` **要入库**：托管方的构建机未必装得了 Python/uv，
  提交生成物才能让「构建」这一步保持廉价。

`docs/` 里不是生成物的那几样，别一起当成产物删掉：

```
docs/assets/          共享资产（图、字体），手写，三棵树共用一份
docs/stylesheets/     手写
docs/javascripts/     手写
docs/downloads/       历史版本归档本体与清单（要入库，见第八节）
```

### 1.2 认领生成物：靠生成横幅，不另存清单

**现象（起初的写法）**：另存一份 `.docsgen.json` 记录「哪些文件是我生成的」，
于是生成之后要同时维护文件和清单两份记录。

**根因**：两份记录会分叉，一份不会。清单里的路径与实际文件不一致时，
清理（prune）会删错文件，或者让过期文件永远留下。

**对策**：判据就是文件里那行生成横幅本身。

* `tools/docsgen.py` 的 `OWNED_RE = ^# ⚠️ 由 tools/docsgen\.py `（markdown 前置元数据里的注释行，
  不会渲染进正文）；
* 跳转桩是 HTML，另立一条 `STUB_OWNED_RE = ^<!-- ⚠️ 由 tools/docsgen\.py `；
* `tools/i18n_check.py` 用同前缀的 `BANNER_RE` 在比对派生语言时剥掉横幅——
  同一处判据，不在第二个文件里重写一遍。

这条判据有个天然的好处：**手写的 `docs/**/*.md` 不带横幅，所以不会被误认领、
也不会被 prune 删掉**。出处与身份是同一件事，不需要额外记账。

### 1.3 生成期与体检期共用同一个函数，别各写一遍

**现象**：`i18n_check` 复核派生语言时要求「产物与重算结果逐字节相等」，
但它自己也要走一遍同样的流水线；两边分开实现之后，一次不一致会表现为
「构建是绿的，体检是红的」，而两边看起来都对。

**对策**：判据只留一处，体检去调生成器的函数。
`tools/i18n_check.py` 直接 `from docsgen import (apply_tags, depth_of, hide_sides, insert_hide,
lang_prefix, rewrite_shared, ...)`；`hide_sides()` 的文档字符串里把这条写明了：
「两边各写一遍，迟早会分叉，而分叉的表现是构建红着、却看不出谁对」。

---

## 二、两条正交轴：版本与语言

### 2.1 版本在前、语言在后

网址是两个维度的拼接：

```
/                     当前版 · 默认语言
/en/                  当前版 · 英文
/0101/                0101 版 · 默认语言
/0101/en/guide/…      0101 版 · 英文
```

**为什么是这个顺序**（`tools/versions.py` 开头写了理由）：为了让人数最多的那一支
（当前版的默认语言）停在**最浅**的地址上。调过来会把它推到 `/zh-hans/` 下面去，
而它是绝大多数读者进来的那一支。

配套的两条：

* **当前版没有目录**——它的内容就在 `content/` 根下，产物落在 `docs/` 根；
  非当前版是 `content/versions/<id>/`，产物落在 `docs/<id>/`；
* **默认语言没有前缀**——它的产物落在树根，其余语言各占一层。

于是某一页的产物位置是 `docs/[<版本>/][<语言>/]<名字>.md`，最多两层前缀。

### 2.2 「去掉语言前缀」不再等于「砍掉前 3 个字符」

**现象**：引入版本轴之后，非当前版的语言判定全部判错，页面被当成默认语言，
站名、标注文案、面包屑一起落到错误的一支——构建不报错。

**根因**：模板里原先在十来个文件里各写一遍 `here[:3] == "en/"`。
在 `0101/en/…` 上 `here[:3]` 是 `010`；在 `archive/en/…` 那种网址上是 `arc`。

**对策**：`overrides/partials/route.html` 是两条切换轴的**唯一判据**，四个宏：

```
route.version(url)   → "" 或 "0101"
route.lang(url)      → "" 或 "en" / "zh-hant"
route.tail(url)      → 两层前缀都去掉之后的页内路径
route.suffix(ldir)   → "" 或 "_en" / "_zh_hant"（配置键名后缀）
```

另有 `code()`（语言 → BCP-47）、`pack()`（语言 → 主题界面文案包名）、
`version_prefix()` / `base()` / `same()` / `anchor()` / `version_label()`。
语言目录与版本清单**都从配置推导**，模板里不留任何一份抄写的判据。

顺带一提，`code()` 单开一个宏是有理由的：`<html lang>`、`og:locale`、`hreflang`、
JSON-LD 的 `inLanguage` 原先各写一遍「是不是英文 / 是不是繁体 / 否则」的三支判断，
加一种语言就得把每一处都补上——而**漏掉的那一处不报错**，页面照常渲染，
只是静静退回默认语言代码，抓取端看到的是错的。

### 2.3 两个切换器各改一层

| 切换器 | 改哪一层 | 保留哪一层 | 模板 |
| --- | --- | --- | --- |
| 版本 | 版本段 | 语言段原样 | `overrides/partials/version.html` |
| 语言 | 语言段 | 版本段原样 | `overrides/partials/alternate.html` |

从 0101 版切回当前版，读者留在同一种语言里；从简体切到英文，读者留在同一版里。
**两层都改就会一次把读者带走两格，那不是他点的。**

这里有一处很容易写错的对称性（`route.html` 里专门写了）：

* **语言切换器**要用 `version_prefix()`（只取版本那一段），不能用 `base()`——
  用 `base()` 会拼出 `zh-hant/en/` 这种根本不存在的地址，而链接看上去还挺像回事；
* **版本切换器**反过来：版本要被替换、语言要留着，所以它自己拼 `vprefix ~ lprefix ~ tail`。

两项目前缀都为空、页内路径也为空时得到的是空串——那是站点根，交给 `nav.homepage.url` 补上。

### 2.4 加一门语言要动哪些地方

语言清单本来是**从 `content/` 的文件名后缀推导**的（`tools/langs.py` 的 `LANG_RE`），
没有一份需要手工维护的语言列表。加一门**手写**语言：

1. `zensical.toml` 的 `[[project.extra.alternate]]` 加一条：`name`（显示名）、
   `link`（网址前缀 `ja/`）、`lang`（BCP-47 `ja`）。页眉语言切换器、404 页的语言列表、
   `hreflang`、`og:locale:alternate`、搜索面板的语言分段**全都读这一条**；
2. 给已有的多语言文案补 `_<lang>` 后缀的键：`site_name_*`、`copyright_*`、
   `site_description_*`、页脚三段、`nav_root_label_*`、`search_*`、
   `download_*`、`status_label.*_*`、以及 `lang_pack_<lang>`；
3. `content/` 里给它写**至少一篇**——一门语言存在与否由文件说了算；
4. 跑 `uv run python tools/i18n_check.py --sync` 把其余缺篇的骨架补齐。

**`lang_pack_<lang>` 是唯一没有推导规则的一处**：主题把简体那一包叫 `zh`
（不是 `zh-Hans`），繁体叫 `zh-Hant`、英文叫 `en`。不能改成「拿语言代码拼模板名、
拼不到再退回主语言子标签」——MiniJinja 的 `import` 没有 `ignore missing`（写了是语法错误），
模板里也没有「这个模板存在吗」的判据，硬拼的结果是**构建期直接失败**。
所以这一处显式写进配置，让它可查（`overrides/partials/route.html` 的 `pack()` 宏，
以及 `zensical.toml` 里的 `lang_pack` / `lang_pack_en` / `lang_pack_zh_hant`）。

另外：`[project.theme] language = "zh"` 是**默认语言**的界面文案包名，加语言时也要看一眼。
主题自带的那几包是「自动生成、不要手改」的上游产物，整包搬进配置只会得到一份会过期的副本——
所以配置里只存包名。

**验收手段**：加完语言，在整库搜这门语言的显示名、语言代码、语言目录名，
搜出来还留在模板或脚本里的，就是漏改。

### 2.5 加一个版本要动哪些地方

1. 把当前版的内容复制成快照：`cp -r content/{index,editor,tables,…} content/versions/<id>/`
   （**不要**把 `content/versions/` 本身复制进去）；
2. `zensical.toml` 的 `[[project.extra.version]]` 加一条：

   ```toml
   [[project.extra.version]]
   id = "0102"
   label = "地编版本 0102"
   date = "2027-01-15"
   ```

3. `make build`。

版本清单**必须写在配置里**，不能像语言那样从内容推导：一个版本**没有任何内容也成立**
（它只是「这一版还没写说明」）。代价是「声明」与「内容」成了两处——
这正是 `tools/versions.py` 的 `audit()` 存在的理由。它守四类错，每一条都以非零码退出：

* 声明了但没有目录；
* 有目录但没声明；
* id 重复；
* 一个 `current` 都没有，或有多个。

`docsgen` 在生成前会跑同一条判据（两边不过就别想生成），`make versions` 是它的单独入口。

### 2.6 版本 id 的两条形状限制

**限制一：不能长得像数字。**

**现象**：`060`、`0100` 这类 id 写进 `.nav.yml` 之后，整个构建以
`nav must be a list` 失败，报错点在主题里，指不到原因。
而 `080`、`091` 完全正常——**只有一部分版本号中招**。

**根因**：YAML 会把光秃秃的 `060`、`0100` 解析成数字（八进制的 0-7 是合法数字），
而 `080`、`091` 里的 8、9 不是八进制数字，于是原样留着字符串。
awesome-nav 拿到手的是一个**数**而不是目录名字符串，找不到那棵树、退回成一个标量，
最后以 `nav must be a list` 让构建失败。

**对策**：`tools/navgen.py` 的 `render()` 里，`_quote_numeric_values()` 把
「长得像数字」的值**无条件加引号**，加完再 `yaml.safe_load` 读回来比对一次
（`if yaml.safe_load(body) != {"nav": nav}: raise SystemExit(...)`）。
引号是无条件加的，不判断「这个值会不会真的被解析成数字」——那种判断本身就是坑源。

**限制二：末段不能带点。**

**根因**：`overrides/partials/trailing-slash.html` 的地址补正脚本会把**末段带 `.`** 的
一律当成文件放过（`objects.inv`、`favicon.svg`、`404.html`）。版本 id 若以点结尾，
补正就失效了。所以 `tools/versions.py` 的 `VERSION_RE` 只允许点出现在中间
（`1.0` 可以，`.hidden` 不行）。

### 2.7 非当前版是冻结快照，缺页由跳转桩兜

非当前版的页面集合与当前版对不上是常态（当前版新加的分区，旧版自然没有），
而版本切换器出现在**每一页**上。所以缺的那些页由 `tools/docsgen.py` 补一个**跳转桩**
（`<meta http-equiv="refresh">`）落到该版本该语言的首页，而不是把读者送进 404。

两个必须知道的细节：

* 桩写成 **HTML** 而不是页面（`.md`）：它要占住 `guide/index.md` 那种会建分区的位置，
  写成页面就会被 `navgen` 当成一个真的分区；
* 桩是生成物，带生成横幅，由 `docsgen` 自己 prune；`tools/linkcheck.py` 会把它的
  refresh 目标当成一条**必须落地的引用**去验。

**桩兜不住的东西：树内部的 markdown 链接。** 这一条是在模版上做版本轴实测时发现的
（把当前版的首页复制进 `content/versions/v0/`，其余页留给桩）：

* 桩落在**产物地址**上（`docs/v0/guide/writing/index.html`），它服务的是
  **浏览器**——版本切换器、跨树导航、别处指过来的地址都能落地；
* 而 `zensical` 解析 `[写内容](guide/writing.md)` 这种**树内部**的 markdown 链接时，
  看的是 `docs/v0/guide/writing.md` 在不在。桩是 `.html`，补不上这一处，
  于是 `--strict` 直接报 `page does not exist`，构建失败；
* 同理，每个版本**自己的** `index.md` 里的 `nav:` 只该列它真有的分区——
  `navgen` 对那些名字会 warn（`nav 中的 “guide” 不存在，已跳过`），
  而 `--strict` 下 warn 会变成失败。

结论：**冻一棵树进 `content/versions/<id>/`，要么整棵复制，要么把它裁剪成自洽的一片**
（nav 只列真有的分区、正文里的链接只指向真有的页）。桩是给「切换器不把人送进 404」
用的，不是给「一棵半截的树也能构建」用的。

### 2.8 空侧栏会自己收掉（判据放在构建层）

**现象**：单页分区（「准备工作」「关于」）与首页没有别的页可铺，左栏是空的，
但空着也占 242 px，正文被挤到 688 px。只有标题、没有小节的页面同理，
右栏渲染出一个点开只有它自己的「目录」。

**对策**：`tools/docsgen.py` 的 `hide_sides()` 给该页的**产物**补一行
`hide: [navigation]`、`hide: [toc]` 或两者兼有（`insert_hide()`）。

**为什么不写在各页的前置元数据里**：分区里加一篇新页、页里加一个小标题，
侧栏该自己回来——写死在前置元数据里就不会回来，而且每加一种语言、每个版本都要各写一遍。
作者自己写了 `hide:` 的，这里不覆盖（那是有意为之，不是推导的结果）。

---

## 三、派生语言（简体 → 繁体）

### 3.1 它不是翻译，所以它不可能过期

繁体是同一份文字的**字符转写**：由 `tools/hant.py` 从简体算出，不单独手写。
于是「简体改了、繁体没跟上」这种状态在设计上就不存在。
判据也因此可以是最硬的那一种：**逐字节等于「对当前简体原文跑一次转换」的结果**
（`tools/i18n_check.py` 的 `inspect_derived()`）。

这条判据要求复核方走**同一条流水线**，顺序都不能变：

```
rewrite_shared(补共享资产的相对层级) → to_hant(转换) → apply_tags → insert_hide → 剥横幅
```

少一步就会误报「派生失同步」，而误报的修法是「跑 `make gen`」——跑完还是不一致，
人就只能去手改产物了。所以 `inspect_derived()` 直接把 `docsgen` 的函数 import 进来用。

不一致时报告里给出 `first_diff`（偏移量 + 前后 30 字的上下文），省得人肉眼找差异。

### 3.2 哪两处不转换

1. **反引号内的内容**——那是真实文件名，必须逐字保留。
   否则 `签到表` 会被转成 `籤到表`（连繁体都转错了，因为这里该是「簽」）。
2. **链接与图片的目标**（`](…)`、`src=`、`href=`、裸 URL、参考式定义）——
   目标里的中文是**路径**，磁盘上的文件名是简体，转它等于把链接指向不存在的文件。
   踩过一次：一个指向 `报告_2026年版.pdf` 的下载链接被转成「報告_2026年版.pdf」，
   文件实际仍叫原名，链接直接取不到。

**但目标里的锚点（`#` 之后那一段）要转。** 这条是后补上的：

```
](settings.md#save说明)    锚点是标题的 id（由 toc.slugify 生成）
## Save说明   →  繁体树里标题转写成 ## Save說明  →  id 变成 save說明
```

锚点若不跟着转，繁体树上**每一条指向中文标题的链接都落空**，而简体树上全都好好的。
`tools/hant.py` 的做法是把路径与锚点分开：`PROTECTED` 整段保住路径位，
`convert_span()` 最后再调 `convert_anchors()` 把 `#` 之后那一段捞出来转写。

### 3.3 转换表的两类修正

* **古体异写归一**（两岸三地都更常用右边那个，不是地区差异）：
  `爲→為 羣→群 啓→啟 祕→秘 峯→峰 衆→眾`，另有 `裏→裡`（转换表对「这里/哪里」给裡、
  对「教室里」给裏，同一篇里会混着）。
* **有地区分歧的字不替读者选边**：`裏/裡` 之外，像 `賬/帳` 这类保持转换表原样。
  这也是为什么语言代码用**文字**标签（`zh-hant`）而不是**地区**标签（`zh-TW`）——
  地区标签会把用词一起改掉（激光→雷射、链接→連結、文件→檔案）。

还有一个必须知道的陷阱（`tools/hant.py` 的 `AMBIGUOUS`）：
**逐字补漏时有些字必须跳过**，因为简繁同形、意思随上下文变：

```
若干→若幹   公里→公裏   皇后→皇後   开采→開採（对）／风采→風采（错）
```

词组表在这些词上是对的，所以补漏那一遍绕开这些字，交给词组表。

最后：**围栏行要单独处理**。```text title="…" 这一行按反引号左右分段会被当成
「行内代码」，于是代码块标题永远停在简体——`convert_span()` 里为它开了一条分支。

---

## 四、主题与引擎的脾气

### 4.1 MiniJinja 缺的东西

**现象**：`RuntimeError: unknown method: string has no method named startswith (in main.html:209)`

**根因**：MiniJinja 的字符串没有 `startswith` / `split` / `find`。

**对策**：

* 判前缀用切片：`{%- if (script.path | d("", true) | string)[:4] != "http" %}`（`overrides/main.html` 的 `libs` 块）；
* 判语言目录、剥锚点这类事一律**直接遍历配置里的列表**，不在模板里把清单拼成字符串再切开
  （`route.html` 的 `lang()`、`code()`、`anchor()`）；
* 真需要扫字符串就逐字符扫一遍（`route.html` 的 `anchor()` 就是这么剥 `#tag:x` 的；
  网址都很短，而且这样不会因为某个标签名里出现 `tags/` 而切错位置）。

### 4.2 `{# … #}` 不嵌套

**现象**：报错是 `undefined has no method named lang`，看着像宏没导入或写错了名字。

**根因**：在 `route.html` 的说明注释里写了 Jinja 的行内注释标记示例，
而 `{#` 与它配对的**第一个** `#}` 之间不嵌套——说明被**提前截断**，
后面那几行就成了真的模板代码。

**对策**：说明里不要出现 `{#` 与 `#}` 这对标记（`route.html` 顶部专门写了这条警告）。
遇到「报错位置与原因完全对不上」的模板错误，先怀疑注释是不是断在半路。

### 4.3 宏返回的是字符串

**现象**：页脚的「上一页 / 下一页」与 `<link rel="prev/next">` **一直没渲染出来**，
而且不报错。

**根因**：判据原先写成布尔再比 `| trim == "True"`，而 MiniJinja 把布尔渲染成
**小写的** `true` / `false`，于是那个比较**恒为假**。

**对策**（`overrides/partials/lang-same.html`）：返回自定义记号 `"yes"`，
不依赖布尔怎么序列化；调用方 `{% if langutil.same_lang(other.url) | trim == "yes" %}`。
空的宏渲染出来是空串，所以宏的返回值一律 `| trim` 再用。

这条踩过之后立了一条**兜底判据**：`tools/i18n_check.py` 的 `SMOKE` 断言表里专门
放了几条守「模板里某个条件是不是恒为假」的断言——`md-footer__link--next`、
`rel="prev"`、`md-path__link`、`md-select__link`、`brand-footer`。
这类哑失败**没有别的办法发现**：不报错、不警告，只有肉眼看页面才知道。

### 4.4 `page.is_homepage` 未定义，一律用 `page.url`

主题 `base.html` 自己也依赖它为空，所以落地页判据只能从网址推：

```jinja
{% set is_home = (route.tail(here) | trim) == "" %}
```

`route.tail()` 去掉版本与语言两层前缀之后为空，就是某一支的首页。
**原先写死三串网址**（`""`、`"en/"`、`"zh-hant/"`），引入版本轴之后非当前版一进来就判错。

### 4.5 404 页是 static template，得显式绕开主模板

**现象**：站点实际上没有 404 页——正文被落地页的内容顶掉。

**根因**：Zensical 把 `404.html` 当作 **static template** 渲染，
主题的 `templates/404.html` 继承 `main.html`；而 404 渲染时并没有页面对象，
`page.url` 同样是空串——于是 `main.html` 里那个落地页判据把 404 判成了首页。

**对策**：`overrides/404.html` 直接继承 `base.html`，自己给出一块正文，
不加载落地页的样式与脚本。与其在主模板里靠「`page.title` 未定义」这类间接线索去区分两者，
不如让 404 走另一条路。

另有两处细节：

* 404 页的语言清单**遍历 `config.extra.alternate`**（与页眉的语言切换器同源），
  原先写死两三条链接，加语言时必漏；
* 循环上用 `{%-` 是为了让产物**逐字节**与改前一样——404 这一块没被 minify 压成一行，
  循环多留一处缩进就会在条目之间多出一个空行。

### 4.6 导航分区：`__lang:` 与 `__ver:`

Zensical 的 nav 是**全站唯一**的（上游只提供 `extra.alternate`，没有任何按语言分 nav 的机制）。
本模版用 awesome-nav 的 `.nav.yml` 描述导航树，并在树里挂一批**从不显示**的分区节点：

```
__lang:en        语言分区（docs/en、docs/0101/en …）
__ver:0101       版本分区（docs/0101 …）
```

两者可以嵌套。标记前缀是**机器可识别的**（模板靠 `"__lang:" in title` 认它），
刻意不用会随人改动的显示名。模板侧三处配合：

* `overrides/partials/nav.html`、`tabs.html` —— 只渲染当前那一支；
* `overrides/partials/path.html` —— 面包屑跳过这两层（否则读者会看到
  「序章 › __lang:en › The Club's Story」这样一串，根还是别种语言的）。

`.nav.yml` 全部由 `tools/navgen.py` 生成，**不要手改**。

### 4.7 导航生成器的两条判据（都踩过）

**坑一：站根遍历会走进别的树。**

**现象**：某个版本目录的 `.nav.yml` 里语言分区没了（语言树下的 `.nav.yml` 干脆一份都没生成），
英文左栏的栏目名按字母序排、标题还尽是中文占位符。

**根因**：站根那一次 `rglob` 会顺手把 `docs/en/`、`docs/0101/` 这些**别的树根**
也收进来，并且是以「没有分区」的样子收的——同一个目录被排两遍，后一遍（空分区）
盖掉前一遍。早先的判据写的是「树根不等于自己就剪掉」，于是站根把
`docs/en/editor/` 这类目录也一并剪掉了——**而站根确实是它们的上级，
上级不是障碍**。

**对策**：判据改成「这个目录**在别的树下面**」，即只剪**下级的**树根
（`tools/navgen.py` 的 `targets()`）。

**坑二：没有页面的树也必须给一份空的 `.nav.yml`。**

**现象**：某个「非当前版 × 某语种」一篇内容都没有（读者由跳转桩送走），
构建以 `nav must be a list` 失败，报错在主题里，看不出是这个语种缺文件。

**根因**：awesome-nav 找不到 `.nav.yml` 就自己去扫目录；扫到一个连 html 都没有的目录，
会把 nav 解析成非列表。

**对策**：这时候**恰恰不能跳过**，写一份空清单（`nav: []` 是合法取值）。

### 4.8 标题锚点

**现象**：目录里的锚点、`toc.permalink` 生成的「¶」全都指不准；
纯中文标题的 id 是 `#_1`、`#_2`……

**根因**：Zensical 默认的 slugify 会把**中日韩字符整段丢掉**：
`## Save说明` → `#save`。

**对策**：`zensical.toml` 里换成 pymdownx 的 Unicode 版：

```toml
toc.slugify.object = "pymdownx.slugs.slugify"
toc.slugify.kwds.case = "lower"
```

于是 `## 一、地编下载后如何配置` → `#一-地编下载后如何配置`。

**同一件事的另一半**：`tools/docsgen.py` 的 `tag_slug()` 必须与主题给页面标签生成的
锚点**逐字符一致**（对不上就是全站标签链接落空），所以它用的是**同一个函数、同一个 case**；
取不到 pymdownx 时退回同规则的手写实现，绝不静默产出别的形状。

**还有一条规矩：需要被链接的标题一律钉显式 ASCII id。**

```markdown
## WallE 说明 { #walle }
```

自动 id 是**标题文字的变形**，靠不住：改一个空格，`#save说明` 就变成 `#save-说明`；
繁体树上同一个标题还会被转写成 `#save說明`。凡是别处要链过去的标题，
都钉一个 ASCII id，链接从此与排版和简繁转换都无关。

---

## 五、Makefile 的坑

### 5.1 裸 `$` 会被 make 吃掉

**现象**：``/bin/sh: unexpected EOF while looking for matching `''``，报错指不到出问题的那一行。

**根因**：make 会把 `$'` 当成「名为 `'` 的变量」吃掉——正则尾巴那个 `$`
连带着收尾的引号一起消失，shell 收到一个不闭合的引号。

**对策**：判据别用带 `$` 尾锚的正则，改用 `grep -qx` 或 `awk`；
真要写 `$`，在 Makefile 里写 `$$`。

### 5.2 假绿灯：判据撞上别处同名的行

**现象**：离线构建产出的地址是目录式的，`file://` 下一层导航全点不动；
而生成配置的那条规则一路放行。

**根因**：判据写的是 `grep -q '^enabled = true'`，而 `[project.plugins.search]`
与 `[project.plugins.tags]` 那两节本来就写着 `enabled = true`——命中的是它们，
offline 那一节压根没改也会通过。

**对策**（`Makefile` 里都写成了注释）：

* 给被 `sed` 改的那一行留一个**行尾锚点注释**（`enabled = false  # 离线`），
  这样「写死 `^enabled = false` 会连别处的同名行一起改中」这件事也一起解决了；
* 判据用 `awk` **限定在那一节里**看：

```make
@awk '/^\[project\.plugins\.offline\]/{getline; if ($$0 == "enabled = true") ok = 1} \
      END{exit !ok}' $@ || { echo "离线配置没打开 offline 插件（那一行的行尾锚点注释还在吗）"; rm -f $@; exit 1; }
```

### 5.3 生成配置的写法：sed + 立刻自检

预览配置与离线配置都由 `sed` 从 `zensical.toml` 生成，而不是「复制一份再改」——
目的是让「除了那一行，两份配置完全相同」这件事**一眼可见**。
生成之后立刻自检，不通过就删掉产物并以非零码退出：

```make
@sed 's|^site_url = .*|site_url = "$(SERVE_ROOT_URL)"|' zensical.toml > $@
@grep -q '^site_url = "$(SERVE_ROOT_URL)"' $@ || { echo "..."; rm -f $@; exit 1; }
```

两份生成的配置都不入 `.gitignore` 之外的任何地方（它们本身在 `.gitignore` 里）。

### 5.4 一条命令序列，本地与 CI 共用

`make build` 与 `.github/workflows/*.yml` 跑的是**同一条命令序列**——
「本地过得去就等于 CI 过得去」。这条的价值在于：CI 里出现一个本地没跑过的步骤时，
它会以「本地绿、CI 红」的形式浪费掉一整轮排查。

### 5.5 `--clean` 与 `serve` 不能同时

`make build` 里 `zensical build --clean` 会清空 `site/`，而 `serve` 正盯着它。
预览中要验证就单独跑：

```
uv run zensical build --strict
uv run python tools/linkcheck.py
uv run python tools/i18n_check.py
```

### 5.6 `serve` 与 `watch` 各管一段

`zensical serve` 只盯 `docs/`（生成物），改 `content/` 下的源文件它看不见。
`tools/watch.py`（`make watch`）轮询手写层的修改时间，一变就跑
`docsgen + navgen`；`zensical serve` 看见 `docs/` 变了会自己重建、自己刷新浏览器。

它盯的是：`content/`、`tools/*.py`、`zensical.toml`、`overrides/`，
以及 `docs/` 下**手写的那几样**（`assets/`、`stylesheets/`、`javascripts/`）；
**不盯** `docs/` 下的 `.md` 与 `.nav.yml`——那是它自己生成的，盯了会自己触发自己。

它只负责「重新生成」，不负责构建与体检。

### 5.7 预览里没有链接体检

`zensical serve` 跳过后处理链，所以预览时看不到 `linkcheck` 的结果——
那是**构建之后才做得了**的事。预览里表现正常、`make build` 报一堆落空链接，
这是正常的，不是体检错了。

---

## 六、链接与资产

### 6.1 相对路径的两种写法

* 指向**站内页面**：相对路径（各语言的目录结构镜像，三语通用）；
* 指向**共享资产**（`docs/assets/`）：也写相对路径，由 `tools/docsgen.py` 的
  `rewrite_shared()` 给非默认语言的产物**多补一层 `../`**，层数由 `depth_of(版本, 语言)` 算。

**这里有一个静默的坑**（在模版里潜伏很久，直到真实内容开始引用 `assets/` 才暴露）：

**现象**：派生语言（繁体）的产物里，指向 `assets/` 的链接少一层 `../`，全是死链；
默认语言一切正常。

**根因**：`render()` 按**源文件**的语言算下沉层数，而源是默认语言（0 层）、
产物是派生语言（1 层）。

**对策**：`render()` 显式接收 `out_lang`（产物这一份的语言），不再从源文件推。
模版自带的示例内容从没引用过 `assets/`，所以这条一直没暴露出来——
**示例内容覆盖不到的东西，就是判据覆盖不到的东西。**

### 6.2 目录地址的尾斜杠

**现象**：读者点某些链接 404，另一些正常；不是整页崩掉。

**根因**：服务器对**不带尾斜杠**的目录地址（`/zh-hant/story/`）也回 200，
而且**不重定向**（GitHub Pages 如此，`zensical serve` 与任何把目录当索引页的静态服务器也如此）。
于是文档停在一个与它自己不一致的基址上：

```
/zh-hant/story  +  href="./gathering-0421/"  →  /zh-hant/gathering-0421/   404
```

**每个向下的相对链接都抬高了一级**。向上的链接反倒没事（多出来的 `..` 在根上被丢掉），
所以读者看到的是「有些链接能点，有些一点就 404」。

**对策**两条腿：

1. **本站自己不生成少斜杠的地址**——由 `tools/linkcheck.py` 的判据 2 守着；
2. **别人拿少斜杠的地址进来也还能用**——每一页在 `<head>` 里最前面补一段地址栏补正
   （`overrides/partials/trailing-slash.html`），`linkcheck` 的判据 4 守着
   （产物里少了 `id="__trailing-slash"` 就直接报错）。

两条是同一件事的两半，**缺一条就重新打开这个坑**。

几处细节，都是踩出来的：

* **为什么改地址而不是加 `<base>`**：`<base>` 必须写**绝对地址**，
  于是本地预览（`127.0.0.1:8000`）与任何非生产域名都会把链接指回线上；
  补地址栏则与主机无关，而且读者此后复制、收藏到的就是规范地址。
* **为什么放在 `<head>` 里最前面**：浏览器是**边解析边解析链接**的，
  脚本要在 `<body>` 之前跑完才能影响正文里相对链接的落点。`extrahead` 是
  `base.html` 中 `<head>` 的最后一块，满足条件；排在它前面的 `<link rel=stylesheet>`、
  `libs` 里的 `<script src>` 走的都是向上的相对路径，本来就不受入口形式影响。
* **只补目录，不动真实文件**：末段带 `.` 的一律放过；目录名本身带点的情形本模版没有，
  真出现时会放过它——那属于「宁可不动」的一侧。
* 这份 partial 被 `main.html` 与 `404.html` **共同引用**（两份模板都继承 `base.html`），
  共用一份免得两边各写一遍。

### 6.3 `site_url` 的子路径是部署形态，不是内容错误

**现象**：全站的绝对引用都被 `linkcheck` / `i18n_check` 判成落空，而它们条条都对。

**根因**：线上若是 GitHub Pages 的**项目站**（仓库名不是 `<用户名>.github.io`），
站点根落在 `/<仓库名>/` 那一层。模板里的 `| url` 过滤会生成 `/repo/editor/`
这样的**绝对**地址，它在站点目录里对应的是 `site/editor/index.html`——
不带 `repo` 那一段。两个脚本原先一律按站点根去解。

**对策**：判据跟着 `site_url` 走。`tools/linkcheck.py` 的 `site_base()` 从
`zensical.toml` 读 `site_url` 的路径部分并剥掉，`i18n_check` 复用同一个函数。

同时立下一项纪律：**`site_url` 必须带着子路径**。少了它，本地看着一切正常，
一上线全是断链——这一项不能按「本地预览方便」来取舍。
预览的方便另想办法（`make serve` 用 sed 生成一份只换 `site_url` 的本地配置，
入口停在根域；`make serve-subpath` 才是核对线上子路径用的）。

### 6.4 404 页的站内链接只能是绝对地址

404 页是**站点根下的单个文件**，而服务器会在**任意路径**下把它拿出来渲染
（`/rwr-mapbook/does/not/exist`）。相对链接会按访客请求的那条路径解析，
所以它的站内引用只能是绝对地址（`/repo/…`）。
这也正是离线包要把 `404.html` **删掉**的原因：离线版没有服务器，
它永远不会被渲染出来，留着只是一页断链。

### 6.5 体检器自己的自检

`tools/linkcheck.py` 在扫一页之前会先数一遍：

```python
for attr in ("href", "src"):
    seen = len(re.findall(attr + r"\s*=", html))
    parsed = len(re.findall(attr + r'\s*=\s*"', html))
    if seen != parsed:
        problems.append(f"...{attr}= 出现 {seen} 次，其中 {parsed} 次是双引号形式——"
                        f"产物属性写法变了，本脚本会漏读，请先修检查器")
```

**体检器最坏的失败方式是安静地放行。** 主题升级、minify 改了属性写法之后，
正则读不到东西，脚本会报告「全部落地」——比不跑更糟。所以凡是用正则读产物的检查器，
都值得有这一类自检。

---

## 七、构建后才知道的东西

`zensical build --strict` 只校验**源文件**里的链接（`[project.validation]` 的
`invalid_links` / `invalid_link_anchors`）。有三类东西在源里看不出来，
只能等构建完之后去产物上查：

1. **模板拼出来的链接**——面包屑、语言/版本切换器、页脚、标签胶囊；
2. **手写的跳转桩**（`docs/*.html` 里 `<meta http-equiv="refresh">` 的目标）；
3. **生成的标签索引**。

它们出问题时构建一声不吭，读者点到才发现。

### 7.1 标签索引：插件没有语言概念

**现象**：默认语言的标签页把别的语言的篇目也一并列了出来——读者选了一种语言，
却在标签页上读到另一种语言的标题。

**根因**：`<!-- material/tags -->` 占位符由 tags 插件在**构建时**展开，
而插件扫的是整个 `docs/` 目录——所有语言的树都在里面。插件本身没有语言概念，
也没法按语言收窄扫描范围。

**两种都可行的对策**（本模版前后用过两种，现在是第二种）：

* **构建后过滤产物**（`tools/tagfilter.py`，模版早期版本）：遍历
  `site/**/tags/index.html`，丢掉指向别的语言树的条目，并把因此空掉的标签分组一并删掉。
  判据是 href 解析后的站点内路径；脚本可重复执行。它的代价是「多一个跑在
  构建之后的步骤」，CI 与 `make build` 都要记得带上它。
* **生成期各语言各生成一份**（现在：`tools/docsgen.py` 的 `tag_index()`）：
  按 `(版本, 语言)` 分组，只收同语言同版本的篇目，落点是 `docs/<lang>/tags/index.md`。
  插件仍然开着（页面标题下的标签、搜索索引里的 `tags` 字段都靠它），
  只是不用它的展开结果。

无论走哪条，**判据都要在构建之后或生成之后**——
放在源级的那一份永远检查不到这个错误。

顺带一条：标签锚点 `{ #tag:<slug> }` 里的 slug 必须与主题生成的锚点逐字符一致，
见 4.8 的 `tag_slug()`。

### 7.2 目录式地址与 `.html` 式地址

同一棵 `docs/` 会拿去构建两种产物（线上目录式、离线 `.html` 式），
于是**所有假定地址形状的检查都只能针对其中一种**。
离线版因此不跑 `linkcheck` 与 `i18n_check`（它们假定 `…/page/`），
改由浏览器实测（见第九节）。

---

## 八、大文件与托管方限制

### 8.1 先把上限问清楚

**现象**：把 26–84 MB 的归档原样放进仓库，git 收得下（单个文件上限 100 MiB），
但部署失败——托管方（这里是 EdgeOne Pages）**单个文件最大 25 MB**。

**结论**：**拦住的是部署那一步，不是 git。** 所以「整包存成一个文件、
让下载器直接抓」这条路在超限时走不通。

上限取十进制：

```python
LIMIT = 25 * 1000 * 1000     # tools/chunker.py
```

理由：托管方说的「25 MB」是哪一种没有明说，**按小的那一头切总是安全的**
（大文件本来就要切）。要调：`make downloads LIMIT=<字节数>`。

> 未解的一处，留给下一个用的人去核对：若托管方的判断是 `>= 25 MB` 而不是 `> 25 MB`，
> 那么最大片正好 25,000,000 B 会被挡在门外，需要留一点余量重切
> （代价是又一份三百多兆的载荷进 git 历史）。切之前先确认清楚。

### 8.2 分片是给「上传」省事，代价落在「下载」

页面上两种情况长得一样，走的路不一样：

| | 仓库里 | 页面上的按钮 | 下载器能用吗 |
| --- | --- | --- | --- |
| 没超上限 | **原样存一份**，还叫本名 | `<a download>`，一条**直链** | 能（IDM、aria2、迅雷右键即可） |
| 超了上限 | `<名字>.partNNN` | 由页面取回、校验、拼装 | **不能** |

**为什么分片那条路下载器用不了**：页面拼装走的是 `blob:` 地址，
那是**页面进程内存里的一个临时句柄**，别的程序拿不到，页面一关就没。
分片本身是真实地址没错，但那是几段，交给下载器只会拿到几段 `.partNNN`，还得自己合并。

所以：**上限设在部署方能接受的最小值之上，文件能整份就整份，只有真超了才切。**

### 8.3 拼装这一侧的判据

`docs/javascripts/downloads.js`：

* **逐片校验**：任何一片的 sha256 对不上就停下报错，不把半个文件塞给读者；
* **整文件再核一次**——那是最强的一条保证；
* 直链那条路不经手，也就无从校验，换来的是下载器能用；
* 存下去的文件名是**原文件名**（`060.zip`，不是一堆 `.partNNN`）；
* `crypto.subtle` 只在安全上下文（https 或 localhost）里存在，取不到就**不校验**，
  照常拼接保存并在状态行里说明——宁可少一道校验，也不要让读者以为下载坏了。

### 8.4 清单是唯一出处

`docs/downloads/manifest.json` 记着路径、字节数、每片与整文件的 sha256。
**页面上不写大小**：大小由脚本从清单里读出来摆在按钮旁边。
手写一份就会在换了源文件之后过期，而**那种过期没有任何症状**。

清单同时是分片名的出处：改了上限之后留下的旧文件，由 `make downloads`
按**上一份清单的名字**清理，不靠文件名去猜。

### 8.5 两边对账

页面上挂按钮的地方是 `<button … data-dl="060">`。`tools/chunker.py` 的 `check()`
两边都算一遍：

* 页面上挂了 `data-dl="060"` 而清单里没有这个归档 → 报错；
* 清单里有归档而没有任何页面引用它 → 报错。

（只扫 `content/` 这个手写层；`docs/` 里的 `.md` 是它的副本，扫那里等于扫两遍。）
和版本清单一个道理：**两处记录会分叉，一处不会。**

### 8.6 体检与生成分开

`make archives`（`tools/chunker.py --check`）**不需要源归档**——
源在群文件里，仓库里没有。它只看仓库里的这一份对不对：分片还在不在、哈希对不对、
页面与清单两边对不对得上。**所以 CI 跑得了它，跑不了 `make downloads`。**

整包与分片都是**最终形态**，不是中间产物，要进仓库。

---

## 九、离线文件版

`make offline` 出一份「解压就能看」的整站（线上与离线两版互不覆盖）。
它用 Zensical 自带的 **offline 插件**，由 `Makefile` 用 sed 从 `zensical.toml`
现生成一份 `zensical.offline.toml`（与预览配置同一套办法）。

### 9.1 与线上版的四处不同

1. **`site_dir` 换成 `site-offline/`**（两个产物不能互相覆盖）；
2. **打开 offline 插件**：它把 `use_directory_urls` 关掉，页面从 `editor/`
   变成 `editor.html`。这一条**是必需的**：`file://` 下面浏览器不会把 `editor/`
   解析到 `editor/index.html`，不关这一项整站导航都点不动——点了就变成浏览器的
   **目录列表**；
3. **删掉 `downloads/`**：归档三百多兆，占整包九成，而离线包本来也下载不了；
   历史版本那一页留着当版本清单看，按钮点不动是意料之中的；
4. **删掉 `404.html`**：理由见 6.4。

### 9.2 跳转桩要**补**一份 `.html` 副本，不是改名

**现象**：离线版里点版本切换器，落到「文件不存在」。

**根因**：`docsgen` 的跳转桩落点是按**线上**的地址形状定的
（`docs/egg/settings/map-settings/index.html`，目录式）；
离线版页址是 `…/map-settings.html`，差一层。
桩的落点没法按构建方式分岔：两版**共用同一棵 `docs/`**，生成期不知道这一份要拿去做什么。

**对策**：`tools/offline_stubs.py` 在**构建之后**动离线产物，
给每个桩**补**一份同内容的 `<名字>.html`，并把里面的跳转目标由目录改写成
该目录的 `index.html`。

**是「补」不是「搬」**：实测两种链法都会出现，而它们要的是不同的形状：

```
prepare/index.html          →  ../egg/prepare/index.html            目录式（| url 加的）
settings/map-settings.html  →  ../egg/settings/map-settings.html    .html 式
```

两种都留着，两种就都落地；少一份就是一处死链。多出来的那几十个文件每个几百字节。

判据是**桩自己那行生成横幅**，不是文件名——名字看不出哪一页是桩、哪一页是真页面。

同样的教训在更早一版上出现过：第一版只把目录式**改名**成 `.html`，
于是 39 处 `../egg/…` 断了；补上目录式副本之后，另 17 处
`../egg/prepare/index.html` 又断了。**「两种写法都在」这件事，是靠实测数出来的，
不是想出来的。**

### 9.3 offline 插件会塞一条外部垫片

打开 offline 插件之后，Zensical 会往 `config.extra.polyfills` 里塞一条
**unpkg 上的** `iframe-worker` 垫片。本模版不引任何外部脚本（见 README 的
「不引任何第三方请求」），离线版更是连不上网，所以 `overrides/main.html` 的
`libs` 块把外部的一律跳过：

```jinja
{%- if (script.path | d("", true) | string)[:4] != "http" %}{{ script | script_tag }}{% endif %}
```

（这一行也是 4.1 里那个 `startswith` 报错的现场。）

### 9.4 离线版里搜索用不了

`search.json` 是 fetch 来的，`file://` 下被跨域策略拦掉——**与垫片无关**。
真要离线也能搜，得把索引内联进 HTML 再改主题的取数逻辑，那是另一件事。
这一条要**写在交付说明里**，不能让读者以为是坏了。

### 9.5 拿浏览器验

离线版不跑 `linkcheck` / `i18n_check`（它们假定目录式地址），
所以它的验收只能靠浏览器实测，见下一节。

---

## 十、搜索面板的多语言

搜索面板是主题用 preact 渲染进一个 **shadow root** 的（挂在 `document.body` 上），
模板插不进去。本站的 `docs/javascripts/search-lang.js` 做的两件事都是**事后摆上去**的：
筛选栏里的标签按语言分段（当前页面语言排最前），面板上写死的英文换成页面语言的词。

### 10.1 三条纪律

1. **定位一律走结构**（`input[role=combobox]` → 父 → 祖父 → …），
   不认它那些**一个字母的类名**——那是压缩产物，升级主题就会变；
2. **主题的节点一个都不搬**（见下，这是踩得最狠的一次）；
3. **结构对不上、配置取不到，就都不做事**——绝不改一半。

### 10.2 搬主题的节点 → 筛选栏越搜越空

**现象**：搜过一次之后标签就剩几个，清空关键字也回不来。

**根因**：原先那一版把主题的原生 `<li>` 搬进自己那几列里——抄是省事，
但那列 DOM 是 preact 的：一搜索，主题就把标签清单按结果重算一遍（只剩命中的几个），
而**我们搬走的那些它既删不掉、也不知道该留**，于是清单越搜越少。

**对策**：只**加一个类**把原生列表藏起来，自己那一列按**当前的原生清单重新派生**
（克隆），点击时**按名字在那一刻解析出原生条目再转发**过去，命中的还是主题自己的逻辑。
写入前先比较签名（`tagName|text|className`），没变就不重摆；
`MutationObserver` 盯着 preact 的重画，对不上就再摆一次。

**两条要澄清的「看起来像 bug」**：

* 主题会按当前的搜索结果**过滤**标签清单（搜「墙」时只剩 5 个标签），
  而且**不清空已选中的标签**——这都是它本来就有的行为，不是缺陷。
  我们要做的只是跟着它变，不要跟它抢 DOM；
* 中文界面下，面板上那几条文案**本来就等于主题的默认英文**——
  别把它当成「我们的脚本没生效」。

### 10.3 判据要跟自己比

搜索冒烟的判据不是「应该是几个标签」（那是主题的过滤逻辑说了算，会随查询变），
而是**parity**：我们那一列必须**始终**与主题那份（被隐藏的）列表条数一致。
主题的过滤决定条数，我们只负责跟得上。

**假警报要追根**：初版有两条断言不过，查下来一条是主题本来的行为、
一条是断言自己写错了（`/en/` 的面板文案等于主题默认值）。
判断手段是**把我们自己的脚本屏蔽掉再跑一遍**——如果现象还在，那就不是我们的问题。

### 10.4 标签属于哪种语言：从索引反查

从 `search.json` 的 `tags` 字段反查，不另立一张表；查不到的标签（刚加、还没进索引的）
归到当前语言那一段。

**索引里的 `location` 是相对站点根的**（`editor/keys/`），判断它属于哪种语言必须
**相对站点根**去解。相对当前页去解会解成 `/editor/keys/en/…`，
语言段被挤出前两段，**于是每个标签都被判成默认语言**。

### 10.5 语言清单从配置来

脚本里**不写语言名**。`overrides/main.html` 在 `<head>` 里放一份

```html
<script id="__rwr" type="application/json">{ … }</script>
```

* `languages` ← `config.extra.alternate`（与页眉的语言切换器**同源**），
  每项有 `name`、`link`、`lang`；
* `ui` ← 按页面语言取好的 `search_*` 文案；
* `download` ← 同理的 `download_*` 文案（给 `downloads.js` 用）。

取不到那份配置就什么都不做——**宁可没有分段，也不要猜错语言名**。
加一种语言因此只动 `zensical.toml`，脚本一行都不用改。

样式要**注进 shadow root**：外面的样式表穿不过去。变量（`--space-*`、`--color-*`、
`--alpha-*`）由面板自己的元素定好，跟着主题走。

---

## 十一、命名撞车

`tools/docsgen.py` 的 `collisions()` 守四类撞车，全都是「生成不报错、
页面悄悄互相覆盖」的那种。前三条在生成时才知道，所以不放进 `versions.audit()`。

1. **版本 id 与 `content/` 顶层的分区目录同名** →
   `docs/<id>/` 会被两棵树同时写入；
2. **版本 id 与 `content/` 顶层的页面同名** → `docs/<id>` 既是目录又是页面；
3. **每个版本都必须有自己的首页**。缺了它，补跳转桩时会生成
   `docs/<版本>/index.html` → 指向 `"./"`，也就是**指向它自己**，读者被卡在
   一个无限自我跳转上。而 `index` 又正是唯一一个「桩的落点等于首页」的名字，
   所以这里**只能报错，不能靠补桩兜住**。
4. **某一版根下的页面名，恰好是另一个版本的 id**（踩得最狠的一次）：

**现象**：离线包里，版本 `egg` 的首页变成了一页「本页无此版本」。

**根因**：页面在「别的版本」里缺页时要补一个跳转桩，桩落在**同一个相对地址**上
（`docs/<目标版本前缀>/<页面名>/index.html`）。于是版本 `egg` 里那篇叫 `egg`
的页面，在每一棵别的版本树里都会生成一份 `docs/egg/index.html`——
而那正是版本 `egg` 自己的目录，和它的首页 `docs/egg/index.md` **抢同一个产物**
`site/egg/index.html`。

**谁赢取决于构建顺序，两版还可能不一样**：线上是首页赢，离线（`use_directory_urls`
关掉）是桩赢。这种「同一条路径两处都要写」的事，生成期就该拦下，不能靠运气。

**对策**：`collisions()` 立判据（页名撞任一版本 id 就报错），**并且**给那一页改名——
页名改了，网址跟着改。

---

## 十二、体检的分工

| 检查 | 守什么 | 为什么在这一层 |
| --- | --- | --- |
| `zensical build --strict` | 源文件里的断链与失效锚点（`validation.invalid_links` / `invalid_link_anchors`） | 源级唯一能做的 |
| `tools/linkcheck.py` | 站内引用落地、目录引用带尾斜杠、跳转桩目标存在、每页都带地址补正脚本 | 这三类都是构建后才定型的链接 |
| `tools/i18n_check.py` | 漏翻 / 过期 / 结构对不上 / 派生失同步 / 产物缺件（SMOKE） | 要同时看内容与产物 |
| `tools/versions.py` | 版本清单与 `content/versions/` 对齐、版本条目里没混进别处的配置 | 声明与内容分成两处，必须对账 |
| `tools/chunker.py --check` | 归档分片与哈希、页面上挂的按钮与清单两边对账 | 只看仓库里这一份，不需要源归档 |

几条要点：

* **体检必须跑在产物上**的理由只有一个：这些错误在源里**看不见**。
* `i18n_check` 的**结构比对**值得单独说：它不数字符，而是比标题层级序列、
  表格行数、提示框数、卡片数、代码块数、图片数、链接集合、HTML 区块数
  （`structure()` / `structure_diff()`）。漏掉一整段、少一行表格、
  把提示框写成普通段落，都拦得住——**这些光看「翻译进度 100%」是看不见的**。
* `SMOKE` 断言表守的是**哑失败**：模板里的条件恒为假时，输出会静默少一块
  （见 4.3）。逐语言各取一页样例（面包屑、页脚下一页、`rel=prev`），
  非当前版各取一页首页；语言清单从 `tools/langs.py` 取，加语言不必回来改这里。
  它只在 `site/` 存在时检查（本地没构建就跳过）。
* **`translation_in_progress` 这条口子**：`zensical.toml` 的 `[project.extra]`
  可以声明哪些语种**还在补齐**。声明了的语种，「缺篇目 / 还是占位 / 刚建骨架」
  只统计不阻塞构建；**「已过期 / 派生失同步 / 未记录指纹 / 结构待核」照样阻塞**。
  这条界线是刻意的：「还没翻」是**看得见**的（那页就是没有），
  「翻了但原文改了」才是**看不见**的——体检存在的理由始终是后者。
* 派生语言的逐字节判据见第三节；不一致时给出 `first_diff` 偏移与上下文。

### 12.1 CI 只校验，不发布

`.github/workflows/docs.yml` 挂在 push 与 PR 上，跑
`make gen` → `docsgen --check` / `navgen --check` → `build --clean --strict` →
`linkcheck` → `i18n_check --quiet`（有归档的站点再加上 `chunker --check`）。
它**不发布**任何东西。

理由写在那个文件里：把一个必然失败、又没有产物的部署步骤留在校验工作流里，
比没有它更糟——它会把「校验过没过」这个**唯一有用的信号**埋在红叉底下。

想发布就另起一条工作流，或者交给托管商从 `main` 构建。

### 12.2 产物推分支：每次一个全新的孤儿提交

**现象（如果往旧分支上追加）**：上一版才有的文件永远留在线上。

**对策**：产物单独起一个仓库 push 上去，每次都是**新的一次提交**再 `-f` 强推：

```bash
cd site
git init -q -b deploy
git add -A -f
git commit -qm "构建产物 · 源提交 ${GITHUB_SHA}"
git push -q -f "https://x-access-token:${TOKEN}@github.com/${GITHUB_REPOSITORY}.git" deploy
```

配套两条：

* `site/` 在 `.gitignore` 里，所以父仓库不会把这里新起的 `.git` 当成嵌套仓库；
* 给工作流配 `concurrency: {group: deploy, cancel-in-progress: true}`——
  两次构建撞在一起时，新的一次顶掉旧的，否则两个 force-push 会互相打架。

**为什么不直接让托管方构建**：这是个 Python 站点（uv + Zensical），
而多数静态托管的构建镜像只预装 Node，安装命令只认 npm/yarn/pnpm。
让它们在容器里现装 uv 再拉一个 Python 并非不可能，但那把「这次能不能发出去」
押在一个没有文档保证的环境上；而这里的构建反而已经被校验工作流每次推送都在验。
所以 `deploy` 分支上**只有成品**，托管那边编译命令与安装命令都留空。

---

## 十三、验证方式：看不见图的时候怎么验

`make build` 跑绿不等于页面对。样式、交互、`file://` 下的行为、
shadow root 里的东西——这些静态检查器一个也验不了。
在没有截图能力的条件下，本实例用的办法是 **headless Chrome + CDP**：

* 起一个带 `--headless --remote-debugging-port=<端口>` 的 Chrome；
* 用 Node（≥ 22，内置 `WebSocket`）写一个几十行的驱动器：
  `fetch http://127.0.0.1:<端口>/json/list` 拿到页面目标 → 连它的
  `webSocketDebuggerUrl` → 收发 `{id, method, params}` 帧；
* 用 `Runtime.evaluate` 取页面上的事实（**返回 JSON 字符串**，别依赖截图）；
* 用 `Page.navigate` 换页；监听 `Runtime.exceptionThrown` 把**控制台报错**也当判据。

### 13.1 冒烟脚本的判据应当是什么

* **静态检查器**（遍历产物、解析引用）能验的只是「文件在不在、引用解不解得开」；
  凡是要点一下才知道的，都要开浏览器点。
* **判据要够严，别把浏览器的兜底页当成通过**。离线冒烟的第一版只看了
  「主题类名在不在、有没有 h1」，结果**浏览器的目录列表页也满足**，
  于是「导航点不动」这个大问题被放过去了。改后的判据是：

  ```
  是真页面 = 主题渲染过 && 地址是 .html && 有 h1 && 一张坏图都没有
  ```

  并且把 URL 与 h1 一起打出来——**判据输出要能让人一眼看出落到哪一页了**。
* **交互式的判据要跟着「别人怎么算」走**：搜索面板的 parity（见 10.3）。
* **假警报要追根**，不要为了让脚本变绿而放宽判据（见 10.2 末尾）。
* 一次冒烟至少覆盖这几样，缺一样就等于没验那一块：
  真页面渲染、图片无一坏、导航点击、正文站内链接点击、
  **切换器点击**（走跳转桩的那条路最容易漏）、控制台无报错。

### 13.2 这些脚本要入库

本实例的冒烟脚本、CDP 驱动器最初都写在 `/tmp` 下，随环境清掉就没了，
而它们验的正是那些**静态体检覆盖不到的东西**。
建议在模版里带上它们（例如 `make smoke`），至少把 `cdp` 驱动器与
「真页面判据」这两件事固化下来——判据比脚本本身重要。

---

## 十四、如果重来一次

把上面踩坑换来的规矩浓缩成一份可以照着做的清单。**顺序不是随意的**：
前面的越靠前，越省事。

**分层与身份**

1. 第一件事就立「产物是生成物」的判据：生成横幅 + `docsgen --check` / `navgen --check`，
   然后再往里放内容。等站大了再补，就要重跑一遍整库确认没有手改过的产物。
2. 删除策略一律用**生成物自己的横幅**认领文件，不另存清单——
   两份记录会分叉，一份不会。
3. 生成期与体检期**共用同一个函数**，不要各写一遍判据。

**两条轴**

4. 两条切换轴的判据**只写一份**（`route.html`），并在模板里禁掉
   `here[:N]` 这类按位置切前缀的写法。语言与版本清单都从配置推导。
5. 模板与脚本里**不出现任何写死的语言名、语言代码、界面文案包名**；
   加语言的验收手段就是「全库搜它」。
6. 版本 id 的形状限制**先于内容**立好：不许长得像数字（`navgen` 无条件加引号），
   不许末段带点（`VERSION_RE`），不许与任何页名或分区目录同名（`collisions()`）。
   每一条都是「生成不报错、页面悄悄错」的类型。
7. 版本清单写在配置里，于是 `audit()` 是**必需品**而不是奢侈品：
   声明与内容分成两处时，两处都要有判据。

**内容与语言**

8. 需要被链接的标题，**从第一篇内容开始**就钉 ASCII id。
9. slugify 一开始就换成 pymdownx 的 Unicode 版，并且让 `tag_slug()` 与它同源。
10. 派生语言用**逐字节**判据，不用「看着一样」；复核方与被复核方走同一条流水线。
11. 保留 `translation_in_progress` 这条口子：只放宽「还没翻」（看得见），
    不放宽「翻了但原文改了」（看不见）。

**构建与部署**

12. 定下 `site_url` 的子路径形态，让**所有**体检跟着它走；
    预览的方便另做一份生成配置，不要去改 `site_url`。
13. 全部站内链接用**相对路径**；唯一的例外是 404 页（绝对地址）——
    并记住离线包要把它剔除。
14. 凡是「构建后才定型」的东西，一律放在构建之后查，
    不要试图在源级假装检查它。
15. 大文件先问清托管方的**单文件上限**，再决定「整份还是分片」；
    分片那条路的判据里必须包含「页面上的按钮与清单两边对账」。

**工程习惯**

16. Makefile 里凡是 `sed` 改配置的地方：**行尾锚点注释 + 生成后自检 + 判据限定在那一节**。
    不要用全库 `grep` 当判据（同名行会给你假绿灯）。
17. Makefile 里不写裸 `$`；要写就写 `$$`。
18. 本地与 CI 跑**同一条命令序列**。
19. 体检器要能自检（比如「产物属性写法变了就报错」）——
    **体检器最坏的失败方式是安静地放行**。
20. 冒烟脚本与判据一起入库，别放 `/tmp`；判据要能区分「真页面」与浏览器兜底页。

**一句话的总纲**：这套模版里所有难修的问题，形状都是同一个——
**不报错，只是悄悄发错东西**。所以判据要立在「错东西已经产生、但读者还没看到」
的那一层，并且每一条都要说得出「它拦住的是哪一种静默失败」。
说不出这一点的检查，多半可以删掉。
